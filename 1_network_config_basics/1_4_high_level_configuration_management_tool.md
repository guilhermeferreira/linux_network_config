# High Level Network Configuration Management Tools


## Netplan

Netplan is available on Ubuntu system.

It converts a YAML file into configuration for either `systemd-networkd` or `NetworkManager`.
